#include <bits/stdc++.h>

using namespace std;

/**
 * Print a vector of strings
 * @param {vector<string> vec}
 * @return Returns nothing
 */
void disp(vector<string> vec)
{
  for (auto str : vec)
  {
    cout << str << endl;
  }
}
